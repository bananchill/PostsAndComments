query SHOW_COMMENTS_QUERY($postId: Int!) {
  showPostComments(postID:$postId){
    id
    owner {
      id
      nickname
      first_name
      last_name
      avatar_url
    }
    post_id
    text
    rating
    parent_id
  }
}

